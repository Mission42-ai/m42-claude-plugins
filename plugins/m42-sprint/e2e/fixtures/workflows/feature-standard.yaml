# Feature Standard Workflow
# TDD sub-phases for implementing features
name: feature-standard
description: Standard red-green-refactor cycle

phases:
  - id: red
    prompt: "Write failing tests for: {{item.prompt}}"

  - id: green
    prompt: "Implement to pass tests for: {{item.prompt}}"

  - id: refactor
    prompt: "Refactor and clean up: {{item.prompt}}"
