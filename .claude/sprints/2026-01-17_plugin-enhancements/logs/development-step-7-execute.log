I have completed the implementation of step-7: Enhanced Error Messages with Recovery Actions.

## Summary of Changes

**File modified:** `plugins/m42-sprint/compiler/src/status-server/page.ts`

### Implementation Details:

1. **CSS Styles** (added after `.error-category-badge.logic`):
   - `.error-details` - container with red-tinted background and border
   - `.error-details-toggle` - expandable button with arrow indicator
   - `.error-details-content` - hidden by default, visible when expanded
   - `.recovery-suggestion` - green-tinted box with lightbulb emoji
   - `.error-message` - monospace formatted error text
   - `.error-message-label` - uppercase label for error section

2. **JavaScript Logic** (added to `getScript()`):
   - `RECOVERY_SUGGESTIONS` mapping with 5 categories:
     - network: "Check internet connection and retry"
     - rate-limit: "Wait a few minutes before retrying"
     - timeout: "Phase took too long - try breaking into smaller steps"
     - validation: "Review input/output requirements"
     - logic: "Review Claude's reasoning in the log"
   - `expandedErrorDetails` Set to track expanded error sections
   - `getRecoverySuggestion(category)` function for lookup

3. **Render Logic** (in `renderTreeNode()`):
   - Added expandable error details section for failed phases with errors
   - Shows recovery suggestion based on error category
   - Displays sanitized error message

4. **Event Handler** (in `updatePhaseTree()`):
   - Toggle handler for `.error-details-toggle` buttons

All 7 gherkin scenarios pass. TypeScript compiles without errors.
